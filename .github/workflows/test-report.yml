name: Test report

on:
  workflow_run:
    workflows: ["Test"]
    types: [completed]

permissions: {}

# Must match the primary Python version in test.yml matrix
env:
  PRIMARY_PYTHON: "3.13"

jobs:
  report:
    name: Publish test results
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion != 'cancelled'
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: write
      checks: write
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@6c3c2f2c1c457b00c10c4848d6f5491db3b629df # v2.18.0
        with:
          egress-policy: audit

      - name: Download test artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4.1.8
        with:
          name: test-results-${{ env.PRIMARY_PYTHON }}
          path: .reports/test/
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish test results (PR annotations)
        uses: EnricoMi/publish-unit-test-result-action@c950f6fb443cb5af20a377fd0dfaa78838901040 # v2.23.0
        with:
          files: .reports/test/test-results.xml
          check_name: "Pytest report (${{ env.PRIMARY_PYTHON }})"
          comment_mode: always
          compare_to_earlier_commit: true
          event_file: ${{ github.event_path }}
          event_name: ${{ github.event.workflow_run.event }}
          commit: ${{ github.event.workflow_run.head_sha }}
